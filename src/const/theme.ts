import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#2da68c',
            contrastText: '#fff'
        },
        secondary: {
            main: '#fff'
        },
        background: {
            default: '#12303b'
        }
    },
});
